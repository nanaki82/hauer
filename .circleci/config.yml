version: 2
jobs:
  build:
    docker:
      - image: elixir:1.9.1-slim

    working_directory: ~/hauer

    steps:
      - checkout

      - run: mix local.hex --force
      - run: mix local.rebar --force

      - run: mix do deps.get, compile

      - run: mix test
      - run: mix credo
